# Core dependency: parsing the .yaml background file format.
PyYAML>=6.0

# Optional: only needed if you set ANTHROPIC_API_KEY to use LLM mode.
anthropic>=0.34.0

# Optional: only needed if you set OPENAI_API_KEY to use LLM mode.
openai>=1.40.0

# Testing
pytest>=7.4.0
